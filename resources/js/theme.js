/**
 * Sipi Falls Admin — Design Tokens
 * Single source of truth for colors used across React/JSX components.
 * Mirrors the CSS variables defined in public/css/style.css
 */
export const colors = {
    primaryGreen: '#228B22',       // Forest Green — main brand
    primaryGreenDark: '#1a6b1a',   // Dark Green — hover states
    primaryGreenDeep: '#0d1f0d',   // Deep Green — footer & dark UI backgrounds
    accentGold: '#E8B923',         // Warm Gold — CTAs and active states
    accentGoldDark: '#c9951a',     // Dark Gold — muted gold variant
    neutralLight: '#F5F6F9',       // Page background
    neutralGray: '#333333',        // Body text
    neutralMuted: '#666666',       // Muted text
    blue: '#3B82F6',               // Info / messages
    teal: '#6FCF97',               // Secondary accent
    red: '#ef4444',                // Error / danger
};
